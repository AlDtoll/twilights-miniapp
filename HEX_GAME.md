# Hex Tactical Battle — описание игры (`game: 'hex'`)

Тактический бой на гексагональной карте 11×11 в стиле FFT. Один герой (+ напарники) против отряда противника. Источник: `index.html`, функции с префиксом `hx*` / `hex*`.

## Цель
Уничтожить всех врагов (или выжить). Результат уходит в чат через `tg.sendData()` (`game:'hex'`, win, turns, урон и т.д.).

## Юниты и характеристики
Дефолты — `HEX_UNIT_DEFS`; герой — из `gameData.hero`. Поля:
- **hp** — здоровье. Шкала: люди 100, Тень 50, Кадавр 85 (можно переопределить per-unit).
- **atk** — базовый урон автоатаки.
- **moveRange** — дальность хода (ОД).
- **atkRange** — дальность базовой атаки.
- **defense** — уклонение (5-10): шанс промаха ПО юниту для дальних/магических атак.
- **statuses** — броня: массив деталей `{name, value, chance}` (см. ниже).
- **shields** — барьеры (заслоны): ряд значений, временные.
- **abilities** — навыки.

## Очки действия (AP)
Герой: 4 AP/ход (`heroAP`). Движение: 1-й гекс бесплатно, далее 1 AP/гекс. Навык стоит `ap` 0/1/2 (`hxAbilityAP`): базовый=1, с кулдауном=2, явное `ab.ap` клампится 0-2. 0 AP — только для навыков с кулдауном.

## Попадание (`hxCalcHitChance`)
- **Ближний бой — всегда 100%.**
- **Дальний/магия**: `база_по_дистанции − уклонение − max(движение 5, укрытие 15)`, кламп 35-95.
  - база по дистанции — на каждую атаку: поле `accByDist:[d1,d2,...]` (точность по гексам; за пределами массива — последнее значение → ограничивает радиус). Иначе дефолт по школе (волшебник мягкий спад, элементалист резкий).
  - движение: цель ходила в этот ход → −5; укрытие (куст/лес/за обломками) → −15; не суммируются.

## Урон (`hxDealDamage`) — порядок защиты
1. **Барьеры (shields)** — ряд заслонов. Любой удар снимает ОДИН передний барьер; проходит `max(0, урон − значение барьера)`. Барьеры пусты в начале, живут 1 ход (сброс в начале хода юнита), сбрасываются при движении. Даются навыком «Заслон» / лесом (15/ход).
2. **Броня (statuses)** — детали `{value, chance}`, НЕ разрушаются. Каждая по очереди кидает `chance%`; сработавшая вычитает `value`. (Шлем 30/15%, Кираса 20/60%, Наручи 10/30%, Поножи 10/15%, Щит 35/20% — наборы по классам.)
3. Остаток → **HP**.

## Навыки и эффекты
Навык = `{id,name,icon,type,range,aoe,damage,effect,maxCooldown,school,accByDist?,ap?}`.
- type: melee | ranged | magic | heal | summon | debuff.
- **Композируемые эффекты** (опционально): `effects:[{type,value,chance?,condition?,to?}]`. См. `MATCH3_MODEL_PORT.md`. Старый формат (`damage`+`effect`) полностью совместим.

## Местность
🌳 Дерево: +1 барьер(15)/ход + −15% точн. дальним. 🗿 Камни: +1 урон/+5% дальним. 🌿 Кусты / 🪨 Обломки: −15% точн. дальним (обломки разрушаемы, дают укрытие). 🧱 Стена: блок движения и обзора (LoS). 🟫 Болото: движение ×2 (без урона). 💧 Вода: непроходимо. 〰 Мелководье: ×2. 🌊 Глубокая вода: нет атаки/способностей. 🔥 Огонь: −20 HP/ход.

## Позиционка
- **Зона контроля**: уход из-под смежного врага провоцирует ⚔ атаку вдогон.
- **Окружение**: каждый лишний смежный атакующий повышает шанс попасть (для дальних) / усиливает давление; у окружённой цели визуальное кольцо.
- **Линия видимости (LoS)**: стены блокируют дальние атаки для всех (и игрока, и врага).

## Данные сцены (через `send_miniapp.py … hex`)
`--hex-hero` (имя, hp, weapon, magic, emoji, level), `--hex-companions`, `--hex-enemy` (type/name, опц. hp/statuses), `--hex-terrain` (rifts/forests/heights), `--hex-walls` ([[col,row]]). Per-unit можно задавать `statuses` и `hp` прямо в JSON.

## Анимации/UI
Пошаговое движение по гексам (`hexPath`), снаряды/удары, оверлей презентации героя с навыками и легендой перед боем, иконки юнитов в верхнем слое, значок местности в углу клетки, инфо-панель с бронёй/щитами/уклонением/AP.
